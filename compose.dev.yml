name: letsy-web

x-web-base: &web-base
  build:
    context: .
    dockerfile: .devcontainer/Dockerfile
    args: { UID: '${UID:-1000}', GID: '${GID:-1000}' }
  image: letsy-web-dev:local
  working_dir: /workspace
  volumes:
    - .:/workspace # source: bind mount -> HMR sees every edit
    - node_modules:/workspace/node_modules
    - pnpm-store:/pnpm-store # location pinned by pnpm-workspace.yaml's storeDir, not an env var
  env_file: [.env]
  networks: [letsy-dev]

services:
  web: # dev server with HMR (CU-CONT-7)
    <<: *web-base
    # A container killed without `astro dev stop` (down, OOM, Docker restart) leaves .astro/dev.json
    # (bind mount) claiming another server is running; a fresh container has its own PID namespace,
    # so the lock is always stale here (adversarial review W-1 H3).
    command: sh -c "rm -f .astro/dev.json && exec corepack pnpm dev"
    environment:
      # Brief W-7 (D-W7-6): dev-only mock of Formspree, served by scripts/dev/waitlist-mock.mjs
      # (a Vite plugin, `apply: 'serve'` -- never present in `astro build`). Overrides whatever
      # (or nothing) .env sets for the `dev`/`e2e` one-shot services, so EarlyAccessForm renders
      # its real <form> here without a live Formspree account.
      PUBLIC_WAITLIST_ENDPOINT: /__dev/waitlist
    ports:
      - '4321:4321'
    mem_limit: 2g
    memswap_limit: 2g
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://localhost:4321/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 20s

  dev: # one-shot commands (install, typecheck, lint, test, build, i18n:check...) and devcontainer shell
    <<: *web-base
    command: sleep infinity
    mem_limit: 2g
    memswap_limit: 2g
    healthcheck:
      test: ['CMD', 'node', '--version']
      interval: 60s
      timeout: 5s
      retries: 3

  e2e: # Playwright, same tag as @playwright/test in package.json (1.62.1)
    image: mcr.microsoft.com/playwright:v1.62.1-noble
    profiles: [e2e]
    working_dir: /workspace
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
      - pnpm-store:/pnpm-store
    environment:
      PLAYWRIGHT_BASE_URL: http://web:4321
    env_file: [.env]
    networks: [letsy-dev]
    command: corepack pnpm e2e
    mem_limit: 2g
    memswap_limit: 2g
    healthcheck:
      test: ['CMD', 'node', '--version']
      interval: 60s
      timeout: 5s
      retries: 3

volumes:
  node_modules: {}
  pnpm-store: {}

networks:
  letsy-dev:
    external: true # created once by scripts/dev/ensure_network.sh (H6)
